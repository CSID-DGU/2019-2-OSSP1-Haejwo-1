app.preloader.show();
app.view.current.router.refreshPage();
app.popup.close();

shortNoti('심부름이 정상적으로 생성되었습니다.');
setTimeout(function(){
  app.view.main.router.refreshPage();
  app.preloader.hide();
}, 500);
