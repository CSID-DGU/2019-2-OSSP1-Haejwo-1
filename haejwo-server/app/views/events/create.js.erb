app.preloader.show();
app.tab.show('#home-tab');
setTimeout(function(){
  $('#chatroom-tab').trigger('click');
}, 100);

shortNoti('심부름이 정상적으로 생성되었습니다.');
setTimeout(function(){
  app.popup.close();
}, 200);
setTimeout(function(){
  app.views.current.router.refreshPage();
  app.views.get('#new_event_popup').router.refreshPage();
}, 500)
